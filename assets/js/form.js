/**
 * Script para o funcionamento do formulário no front-end
 * 
 * Observação: A maior parte da lógica é injetada diretamente no template para garantir
 * que cada formulário na mesma página funcione independentemente.
 * Este arquivo existe principalmente para definir o objeto brazdigital_forms.
 */
(function($) {
  'use strict';
  
  // Quando o DOM estiver pronto
  $(document).ready(function() {
      // O objeto brazdigital_forms é definido no wp_localize_script na classe BrazDigital_Forms
      
      // Para uso futuro: adicionar funcionalidades extras aqui, se necessário
  });
  
})(jQuery);